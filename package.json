{
  "name": "i8-frontend",
  "version": "1.0.0",
  "description": "Insightful Eight Ltd Portal Frontend - PWA",
  "scripts": {
    "start": "npx serve . -p 3000 --single",
    "build": "html-minifier --collapse-whitespace --remove-comments --minify-css true index.html -o index.html && echo 'Minified index.html - Ready'",
    "preview": "npm run build && npx serve ."
  },
  "devDependencies": {
    "serve": "^14.2.1",
    "html-minifier": "^4.0.0"
  }
}
