{
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.js" },
    { "source": "/(.*)", "destination": "/index.html" }  // SPA fallback for client-side routing if needed
  ],
  "functions": {
    "api/index.js": {
      "runtime": "nodejs24.x",
      "maxDuration": 30
    }
  }
}
